import Container from "react-bootstrap/Container";
import "./index.css";

function DailyJournal() {
  return <Container>Here there would be editor with current date , with plugin editor, encrypt when sent to backend, you cannot change the date here</Container>;
}

export default DailyJournal;
